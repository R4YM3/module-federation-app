import React from "react";
import ReactDOM from "react-dom";

const Account = () => (
  <section>
    <header>
      <h2>Account</h2>
    </header>
    some account information
  </section>
);

export default Account;
